# ShokaX 博客工作指南

## 架构总览

```
你写文章 ──→ Sveltia CMS ──→ GitHub (source 分支)
                                    │
                              GitHub Actions
                              (自动 hexo generate)
                                    │
                                    ▼
                              GitHub (main 分支)
                                    │
                              GitHub Pages
                                    │
                                    ▼
                         https://chenlan7064.github.io
```

## 日常写作

### 方式一：Sveltia CMS（推荐）

1. 打开 **https://chenlan7064.github.io/admin/**
2. 登录（GitHub OAuth 或 Token）
3. 点击 **"文章"** → **"新建文章"**
4. 填写表单：
   - 标题、标签、分类
   - 封面图（可拖拽上传到 `/images/`）
   - 摘要描述
   - Markdown 正文
5. 点击 **保存** → 自动 commit 到 GitHub `source` 分支
6. GitHub Actions 自动构建部署 → 1-2 分钟后线上更新

### 方式二：本地命令行

```bash
cd "D:\博客\Blog\ShokaX"

# 创建文章
hexo new "文章标题"
hexo new tech "技术文章"   # 技术模板
hexo new diary "生活记录"  # 日记模板

# 编辑 source/_posts/ 下的 .md 文件
# ...

# 提交到 GitHub（触发自动部署）
git add -A
git commit -m "新文章: xxx"
git push origin source
```

### 方式三：本地部署（离线使用）

```bash
cd "D:\博客\Blog\ShokaX"

# 本地预览
hexo server
# 浏览器打开 http://localhost:4000

# 手动构建 + 部署（跳过 Actions）
hexo clean && hexo generate && hexo deploy
```

## 文章 Front-Matter 参考

```yaml
---
title: 文章标题
date: 2026-05-29 20:00:00
updated: 2026-05-30 10:00:00
tags:
  - 标签A
  - 标签B
categories: 技术
cover: /images/my-cover.jpg
description: 这篇文章讲的是...
sticky: false
---
```

| 字段 | 说明 |
|------|------|
| `title` | 文章标题 |
| `date` | 发布日期 |
| `tags` | 标签列表 |
| `categories` | 分类（支持多级：`技术/前端`） |
| `cover` | 封面图 URL 或本地路径 |
| `description` | 摘要（留空则自动取正文前段） |
| `sticky` | `true` 置顶 |

## 项目结构

```
D:\博客\Blog\ShokaX\
├── _config.yml              # Hexo 主配置
├── _config.shokax.yml       # ShokaX 主题配置
├── source/
│   ├── _posts/              # ★ 文章存放处
│   ├── _data/
│   │   ├── images.yml       # 封面图列表（至少6张）
│   │   └── assets/          # 头像等资源
│   ├── images/              # 上传的图片
│   └── admin/               # CMS 配置文件
│       ├── index.html
│       └── config.yml
├── scaffolds/               # 文章模板
│   ├── post.md              # 通用模板
│   ├── tech.md              # 技术文章模板
│   └── diary.md             # 日记模板
├── .github/workflows/
│   └── deploy.yml           # 自动构建部署脚本
└── public/                  # 构建产物（git ignored）
```

## 常用命令速查

```bash
hexo server              # 启动本地预览 :4000
hexo new "标题"          # 新建文章
hexo new draft "标题"     # 新建草稿
hexo publish "标题"       # 发布草稿
hexo clean               # 清理缓存
hexo generate            # 生成静态文件
hexo deploy              # 手动部署到 GitHub Pages
```

## 修改站点信息

- **站点标题/作者/语言** → 编辑 `_config.yml`
- **导航菜单/社交链接/评论** → 编辑 `_config.shokax.yml`
- **主题色/封面/字体/暗色模式** → 编辑 `_config.shokax.yml`
- **封面图片** → 编辑 `source/_data/images.yml`
